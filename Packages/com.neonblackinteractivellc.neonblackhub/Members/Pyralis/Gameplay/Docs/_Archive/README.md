# Pyralis Docs Archive

This folder contains dated plans, audits, and historical notes that are no longer active source-of-truth guidance.

Use active docs for current setup, architecture, validation, and roadmap decisions. Archive files are retained only as historical reference or planning receipts.

If an archived note becomes current again, rewrite the relevant active doc directly instead of linking future work back into the archive.
